# v0.2.1-foundation-freeze

**Release date:** 2026-07-08  
**Tag:** `v0.2.1-foundation-freeze`  
**Commit:** `54959ae`

Foundation freeze — stable baseline before P2-M2 (BC-01). Phase 1 platform infrastructure, P2-M1 BC-10 Identity & Access Core, Pre-P2-M2 Hardening Sprint, and tenant isolation fix.

---

## Highlights

- **76 automated tests** passing (7 architecture + 47 shared kernel + 22 host)
- **Clean Architecture** enforced by NetArchTest in CI
- **Multi-tenant identity** with fail-closed API authorization and tenant-scoped execution context
- **Event-driven integration** via MassTransit with consumer idempotency
- **Foundation frozen** — no further refactoring until P2-M2

---

## Features

### Phase 1 — Platform (M1–M6)

- Clean Architecture solution (.NET 9)
- Platform runtime: execution context, plugin loader, agent registry, workflow runtime
- API hardening: RFC 7807 Problem Details, structured configuration validation
- Agent SDK contracts and agent type catalog
- Messaging: MassTransit, idempotency, dead-letter sink
- AI provider abstraction foundation (no external providers yet)
- Minimal Next.js admin shell
- Docker Compose stack and GitHub Actions CI

### P2-M1 — BC-10 Identity & Access Core

- Domain aggregates: `Tenant`, `User`, `Role`, `Permission`
- CQRS commands/queries: provision/suspend tenant, create user/role, assign role/permission
- EF Core persistence with migrations
- HTTP API under `/api/v1/identity/*`
- Integration events: `TenantProvisioned`, `TenantSuspended`

### Pre-P2-M2 Hardening (H-01–H-09)

- Identity FK constraints migration
- `ITenantScope` + `TenantHeaderMiddleware` tenant filtering
- Expanded architecture tests (7 rules)
- Consumer idempotency ordering fix
- Plugin discovery cached at startup
- Runtime demo endpoint secured outside Development
- Plugin signature enforcement (`AllowUnsignedPluginsDev`)
- `IdentityDomainEventDispatcher`
- Extended BC-10 regression tests

### Security fix — AUD-DM-01

- Tenant isolation **fail-closed**: tenant-scoped mutations require matching `X-Tenant-Id` header
- Platform-scoped operations remain header-optional (provision tenant, list all tenants)

---

## Tests

| Suite | Count |
|-------|-------|
| AIPM.Architecture.Tests | 7 |
| AIPM.SharedKernel.Tests | 47 |
| AIPM.Host.Tests | 22 |
| **Total** | **76** |

### Coverage (Release build)

| Metric | Value |
|--------|-------|
| Line coverage | ~75% |
| Branch coverage | ~51% |

---

## Architecture

- **Pattern:** Clean Architecture + DDD + CQRS (MediatR)
- **Bounded context delivered:** BC-10 Identity & Access
- **Architecture tests:** Domain↛Infrastructure, Application↛Host, Application↛Infrastructure, and more
- **Blueprint authority:** `Engineering-Blueprint/` (SRS, SAD, EIM, Domain Model, ADRs)

---

## Known limitations

| Item | Notes |
|------|-------|
| SQLite identity store (dev) | PostgreSQL path per ADR-TECH-001 deferred |
| API key auth only | Enterprise SSO/IdP deferred |
| No transactional outbox | Events published inline |
| In-memory consumer idempotency | Non-durable; concurrent race possible |
| Plugin signature presence-only | No cryptographic verification |
| OpenTelemetry OTLP NU1902 | Moderate advisory; upgrade deferred |
| No login UI | BC-10 minimal scope for P2-M1 |

---

## Upgrade from v0.2.0

1. Pull `v0.2.1-foundation-freeze`
2. Run `dotnet ef database update` (new FK migration)
3. Tenant-scoped identity operations now require `X-Tenant-Id` header

---

## Full changelog

[CHANGELOG.md](../../CHANGELOG.md#021--2026-07-08)
